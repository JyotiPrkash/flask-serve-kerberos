# flask-serve-kerberos

- run python init_db.py  //to initialize the DB

If remote DB already exists, then connect DB in app.py & twerk DB keys in code

- run python app.py (--port xxxx). // to run the app in local

